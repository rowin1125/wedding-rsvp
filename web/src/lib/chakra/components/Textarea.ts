import { defineStyleConfig } from '@chakra-ui/react';

export const Textarea = defineStyleConfig({
    baseStyle: {
        _placeholder: {
            color: 'gray.200',
            fontSize: '16px',
        },
    },
});
